
# Domain Enum

Modulo para enumeracion basica de dominio objetivo.

## Objetivo

- Resolver dominio principal.
- Obtener informacion inicial util para etapas posteriores.

## Salida

El modulo debe devolver JSON normalizado con `schema_version`, `target`, `results` y `metrics`.
